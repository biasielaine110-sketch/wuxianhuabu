# Install lifecycle scripts are managed at the repo root.
# protobufjs postinstall is advisory-only; fsevents is darwin-optional.
# Skipping them removes npm allow-scripts advisory noise on Vercel/CI.
ignore-scripts=true
