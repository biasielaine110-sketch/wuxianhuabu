# ========== 本地纯前端（默认）==========
# 不设 VITE_BACKEND_ORIGIN 即可；Gemini API Key / OpenAI 兼容(ToAPIs) 直连浏览器。
#
# 若本地也要调 Vertex，请：1) 另开终端 `npm run dev-backend`  2) 在此启用下行（无尾斜杠）：
# VITE_BACKEND_ORIGIN=http://127.0.0.1:5000

# ========== Vercel / 生产构建（在 Vercel → Settings → Environment Variables 配置，改完需 Redeploy）==========
# 仅在使用 Vertex 时需要：Node 后端公网根地址，无末尾斜杠。
# VITE_BACKEND_ORIGIN=https://你的后端.up.railway.app
#
# 与后端 PROXY_HEADER 必须完全相同（backend/.env.local 里的同一串）。不设则使用前端内置默认值（仅便于本地）。
# VITE_PROXY_HEADER=与后端一致的密钥

# 站内密码页（SiteAccessGate）：本地用 frontend/.env.local；Vercel 在 Environment Variables 设 VITE_SITE_PASSWORD 后必须 Redeploy（构建时写入 JS）。
# monorepo 下 vite 已固定从本目录读 env 并合并 process.env，避免「线上变量有了但仍无密码框」。
# VITE_SITE_PASSWORD=

# Vercel Edge 浏览器弹窗认证（middleware.js），可与站内密码叠用。
# BASIC_AUTH_PASSWORD=
# BASIC_AUTH_USER=
