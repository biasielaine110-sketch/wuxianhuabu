# ========== 本地纯前端（默认）==========
# 不设 VITE_BACKEND_ORIGIN 即可；Gemini API Key / OpenAI 兼容(ToAPIs) 直连浏览器。
#
# ========== GCP Vertex 赠金通道（gemini-*-gcp 模型）==========
#
# 【方式 A — 推荐本地】Vite 已将 /api-proxy 代理到 127.0.0.1:5000，只需：
# 1) 复制 backend/.env.local.example → backend/.env.local，配置 GOOGLE_APPLICATION_CREDENTIALS
# 2) 另开终端 `npm run dev-backend`（或根目录 `npm run dev:vertex` 同时启前后端）
# 无需设置 VITE_BACKEND_ORIGIN。
#
# 【方式 B — 显式指定后端】若 backend 不在 5000 端口：
# VITE_BACKEND_ORIGIN=http://127.0.0.1:5000
# VITE_GOOGLE_CLOUD_PROJECT=project-59d87bf6-dd18-4439-ab6
# VITE_GOOGLE_CLOUD_LOCATION=us-central1
#
# ========== Vercel / 生产（无需独立 Node 后端）==========
# 前端部署后默认同源请求 /api-proxy，由 Vercel Serverless 鉴权转发 Vertex。
# 在 Vercel → Settings → Environment Variables 配置（改完需 Redeploy）：
#
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}   # 整段 JSON 一行粘贴
# GOOGLE_CLOUD_PROJECT=project-59d87bf6-dd18-4439-ab6
# GOOGLE_CLOUD_LOCATION=us-central1
# PROXY_HEADER=与下方 VITE_PROXY_HEADER 相同
# VITE_PROXY_HEADER=自定义密钥（须与 PROXY_HEADER 一致）
#
# 可选：若仍使用外部 Railway/自建 Node 后端，可设：
# VITE_BACKEND_ORIGIN=https://你的后端.up.railway.app

# 站内密码页（SiteAccessGate）：本地用 frontend/.env.local；Vercel 在 Environment Variables 设 VITE_SITE_PASSWORD 后必须 Redeploy（构建时写入 JS）。
# VITE_SITE_PASSWORD=
