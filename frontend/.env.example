# 仅生产构建需要：在 Vercel 的 Environment Variables 里设置同名变量（Production）
# 填你部署的 Node 后端公网根地址，不要末尾斜杠。留空则走当前站点同源的 /api-proxy（本地 + 同域反代）
# VITE_BACKEND_ORIGIN=https://你的后端.up.railway.app

# Vercel 整站访问密码（HTTP Basic）：在项目 Settings → Environment Variables → Production 添加
# BASIC_AUTH_USER=
# BASIC_AUTH_PASSWORD=
